mkdir -p temporal_videos
mkdir -p APP-Database

if [ ! -f ./.env ]; then
    echo CAMERAI_IMAGES=/Images >> ./.env
    echo DATABASE=/app/APP-Database/camerai.db >> ./.env
    echo TEMPORAL_VIDEOS=/app/temporal_videos >> ./.env
fi
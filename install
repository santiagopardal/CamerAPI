mkdir -p temporal_videos

if [ ! -f ./.env ]; then
    echo CAMERAI_IMAGES=/Images >> ./.env
    echo DATABASE=/camerapi/camerai.db >> ./.env
    echo TEMPORAL_VIDEOS=/camerapi/temporal_videos >> ./.env
fi